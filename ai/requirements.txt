# Core image/OCR dependencies
Pillow>=10,<12
pytesseract>=0.3.13,<0.4

# Face detection is optional at import time; install for face verification.
opencv-python-headless>=4.10,<5

# Optional face embedding backend. Install only if the deployment environment
# can support its transitive dependencies/model downloads.
# deepface>=0.0.93,<0.1
